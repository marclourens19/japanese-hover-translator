* text=auto eol=lf

# Large bundled binaries -- tracked via Git LFS so a normal clone stays fast
# while still including everything needed to run the app out of the box.
*.sqlite3 filter=lfs diff=lfs merge=lfs -text
models/**/*.bin filter=lfs diff=lfs merge=lfs -text
